import { ConditionIconPipe } from './condition-icon.pipe';

describe('ConditionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new ConditionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
