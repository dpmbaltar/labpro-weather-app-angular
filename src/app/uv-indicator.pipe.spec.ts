import { UvIndicatorPipe } from './uv-indicator.pipe';

describe('UvIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new UvIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
